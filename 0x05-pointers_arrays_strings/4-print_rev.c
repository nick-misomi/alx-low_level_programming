#include <string.h>
#include "main.h"

/**
*print_rev - prints a string in reverse order
*@s: string to be printed in reverse
*return: nothing
*/

void print_rev(char *s)
{
	int n;

	n = strlen(s);

	for (i = n-1; 1 >= 0 i--)
	{
		_putchar(s[i]);
	}
}
