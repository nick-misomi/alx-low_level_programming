#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 * main - print if the number is positive, zero or negative then
 * returns 0
 */

int main(void)
{
char sh;
for (sh = 'a'; sh <= 'z'; sh++)
{
	putchar(sh);
}
putchar('\n');
return (0);
}
